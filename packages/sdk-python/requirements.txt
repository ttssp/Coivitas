# Runtime dependencies (kept in sync with pyproject.toml [project.dependencies]).
# Security-relevant dependency list.
pydantic>=2.6.0,<3
# cryptography: crypto backend, Ed25519 signature verification in interop tests.
cryptography>=42.0
# authlib: JWT/OAuth2 verifier.
authlib>=1.3,<2
# jcs: a Python binding for RFC 8785 JCS, keeping cross-lang canonicalization consistent.
jcs>=0.2,<0.3
# requests: HTTP client for the OAuth2 introspection verifier.
requests>=2.31,<3
