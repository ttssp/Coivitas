{
 "_comment": "sdk v0.2 Case 4: JWT verify failure — expired token",
 "verifierKind": "jwt",
 "scenario": "fail_expired",
 "expectedError": "SDK_JWT_VERIFY_FAILED",
 "_note": "JWT exp=1000 (Unix epoch 1970-01-01) → expired → SDK_JWT_VERIFY_FAILED"
}
